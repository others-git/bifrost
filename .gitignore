/target
/.env
/references/

# Runtime database (created on first run)
bifrost.db

# Frontend build output — rebuilt by CI and Docker; not committed
/frontend/dist/

# Node dependencies
/frontend/node_modules/

# Local dev runtime data
/data/
TODO

# Local dev server launcher (holds BIFROST_SECRET) — never commit
/dev.sh

# MkDocs build output (docs site)
/site/
